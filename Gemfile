source "https://rubygems.org"
ruby RUBY_VERSION

# This will help ensure the proper Jekyll version is running.
gem "jekyll", "3.1.6"

group :jekyll_plugins do
  gem 'jekyll-gitlab-letsencrypt', :github => "Sotilrac/jekyll-gitlab-letsencrypt"
end
