---
layout: layouts/standalone.njk
pagination:
  data: calcList
  size: 1
  alias: calc
permalink: "/calc/{{ calc.name }}/"
eleventyComputed:
  title: "{{ calc.title }}"
  manifest: "/calc/{{ calc.name }}/manifest.webmanifest"
  themeColor: "{{ calc.theme }}"
---

<{{ calc.tag }}></{{ calc.tag }}>
<script src="{{ calc.script }}" defer></script>
